// The cursor light-trail effect has been removed from the site.
// This file is intentionally left as an inert placeholder because this
// tool doesn't have delete access to your filesystem — nothing imports
// it anymore (removed from App.jsx), so it has zero effect either way.
// Safe to delete this file manually whenever you like.

export default function CursorTrail() {
  return null;
}
